
const sentence = ["The", "quick", "brown", "fox", "jumps", "over", "the", "lazy", "dog"];
// eslint-disable-next-line no-unused-vars
const sentenceUppercase = sentence.map((words) => words.toUpperCase());

console.log(sentenceUppercase);


promise.then(() => {
  console.log();
  if (words != '') {
    reject
  } else {
    resolve
  }
  
  resolve
});

console.log(promise);


